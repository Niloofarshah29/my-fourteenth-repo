# my-fourteenth-repo
3. Reverse a String (C++)
#include <iostream>
#include <algorithm>
usin namespace std;

int main() {
    string str = "OpenAI";
    reverse(str.begin(), str.end());
    cout << str << endl;
    return 0;
}
________________________________________
